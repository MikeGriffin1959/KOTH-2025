files:
  "/etc/environment":
    mode: "000644"
    owner: root
    group: root
    content: |
      ZONE=America/New_York

commands:
  01_set_timezone:
    command: |
      ln -sf /usr/share/zoneinfo/$ZONE /etc/localtime
      echo $ZONE > /etc/timezone
